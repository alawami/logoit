### LogoIt

This python program adds logos to all pictures in directory.

To run on windows (on the run dialog) add the scripts folder to the system path:
Start > Type: Edit environment variables for your account > Select Path variable > Click Edit > In the value text append a semicolon and type python scripts folder path.
